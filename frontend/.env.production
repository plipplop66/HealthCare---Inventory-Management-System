# Isolated test-branch-2 deployment: set the NEW API URL in Vercel before building.
# Fail locally at this origin if omitted; never fall back to the working production API.
VITE_API_BASE_URL=/api
VITE_USE_MOCKS=false
